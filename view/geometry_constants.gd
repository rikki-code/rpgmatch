## Shared sizing constants for view code (not pack-specific — see PackPaths for that).
class_name GeometryConstants
extends RefCounted

const CELL_SIZE := 1.0
const DEFAULT_TILE_RADIUS := CELL_SIZE * 0.36
